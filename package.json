{
  "name": "mic-boost",
  "private": true,
  "version": "1.0.0",
  "description": "Android microphone monitor and processor with gain, noise gate and compressor controls.",
  "scripts": {
    "build": "npm run build:web",
    "build:web": "vite build",
    "cap:sync": "npx cap sync android",
    "cap:add": "npx cap add android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/cli": "^8.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.0",
    "vite": "^8.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
